[pytest]
addopts = -ra

markers =
    db: database tests
    integration: integration tests (API → DB or API → API layer validation)
    security: security tests (JWT tampering, injection, access control)